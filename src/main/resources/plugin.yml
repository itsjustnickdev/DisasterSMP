name: DisasterSMP
version: 1.0
main: org.example.DisasterPlugin
api-version: 1.21
folia-supported: true
commands:
  disaster:
    description: Start een ramp
    usage: /disaster
permissions:
  disaster.trigger:
    description: Toestemming om rampen te activeren
    default: op # Alleen operators krijgen dit standaard

